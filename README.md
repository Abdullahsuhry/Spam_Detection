📨 Spam Message Detection using Machine Learning

A machine learning-based system that classifies text messages as Spam or Not Spam (Ham). The project uses text preprocessing, TF-IDF vectorization, and a trained ML model to detect unwanted messages.
A simple Flask web app is included so users can type a message and instantly see whether it is spam or not.

🌟 Features

✅ Cleaned and preprocessed SMS dataset

✅ TF-IDF vectorizer for text feature extraction

✅ Trained ML model (Logistic Regression / Naive Bayes)

✅ User-friendly Flask web interface

✅ Real-time spam prediction
